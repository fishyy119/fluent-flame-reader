// Types used not by the schema, but still free to import into both main and renderer scripts.

export type CustomArgs = {
    // Force using the window frame, regardless of platform.
    forceFrame: boolean;
};
